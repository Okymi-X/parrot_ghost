---
- name: "Install Packages"
  package:
    name: "auditd"
    state: present
  become: true
  become_method: sudo

- name: "Configure Audit Rules"
  copy:
    src: audit.rules
    dest: /etc/audit/rules.d/audit.rules
    owner: root
    group: root
    mode: '0640'
  become: true
  become_method: sudo

- name: "Create _laurel user"
  user:
    name: _laurel
    state: present
    shell: /bin/false
    create_home: no
    home: /var/log/laurel
  become: true
  become_method: sudo

- name: "Create directories for _laurel /var/log/laurel"
  file:
    path: "{{ item.path }}"
    state: directory
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0750') }}"
  loop:
    - { "path": "/var/log/laurel", "owner": "_laurel", "group": "_laurel" }
    - { "path": "/tmp/laurel", "owner": "_laurel", "group": "_laurel" }
    - { "path":"/etc/laurel" }
  become: true
  become_method: sudo

- name: "Copy laurel/config.toml to /etc/laurel/config.toml"
  copy:
    src: laurel/config.toml
    dest: /etc/laurel/config.toml
    owner: _laurel
    group: _laurel
    mode: '0640'
  become: true
  become_method: sudo

- name: "Copy laurel/laurel.conf to /etc/audit/plugins.d"
  copy:
    src: laurel/laurel.conf
    dest: /etc/audit/plugins.d/laurel.conf
    owner: root
    group: root
    mode: '0640'
  become: true
  become_method: sudo

- name: "Downloading laurel release"
  get_url:
    url: https://github.com/threathunters-io/laurel/releases/download/v0.5.2/laurel-v0.5.2-x86_64-glibc.tar.gz
    dest: /tmp/laurel-v0.5.2-x86_64-glibc.tar.gz
    mode: '0640'
  ignore_errors: true

- name: "Create temporary laurel extraction directory"
  file:
    path: /tmp/laurel
    state: directory
    mode: '0755'
  become: true
  become_method: sudo

- name: "Extract laurel release"
  unarchive:
    src: /tmp/laurel-v0.5.2-x86_64-glibc.tar.gz
    dest: /tmp/laurel/
    remote_src: yes
    owner: root
    group: root
    mode: '0755'
  become: true
  become_method: sudo
  ignore_errors: true

- name: "Install laurel binary"
  command: "install -m755 laurel /usr/local/sbin/laurel"
  args:
    chdir: /tmp/laurel/
  become: true
  become_method: sudo
  ignore_errors: true

- name: "Restart auditd"
  service:
    name: auditd
    state: restarted
  become: true
  become_method: sudo
  ignore_errors: true
